considerar analizis de sentimiento para más adelante
usar embedding para transformar texto en números para pasarlo por el modelo de regresión logistica

TF-IDF para convertir en numérico los textos/lematizados
keyword -> encoder / one hot encoder / binary encoder

train
- A
- B
- C
- DEFAULT

test
- A
- D -> DEFAULT xq no lo vio en train

word2vec para hacer los embeddings y luego procesarlo con un random_forest/regresión_logistica/etc.

hacer un tuneo de parametros (random search para baseline) y (grid search para los otros modelos)

Opciones para modelos avanzados
- Árboles
- XGBoost
- Redes Neuronales (keras) -> puede servir para un encoding 

TP Opcional:
- Clustering
- Modelos de reducción de dimensiones